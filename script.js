
  let picColor = document.querySelectorAll('.color');
  picColor.addEventListener('click', function()) {
    for( let index = 1; index < picColor.length; index += 1){
      if(picColor[index].contains('selected')){
        
      }
    }
  }
